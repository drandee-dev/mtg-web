# Web framework
fastapi>=0.115
uvicorn[standard]>=0.30

# AI features (Phase 2: Rules Q&A, deck suggestions)
anthropic>=0.52

# Auth (Supabase JWT verification) + AI usage tracking (Supabase table)
pyjwt>=2.8
supabase>=2.9

# Pulled in transitively by mtg_utils (the reused deck/card/rules library)
click>=8.0
requests>=2.28

# ReDoS guard: timeout-capable engine for user-supplied regexes
# (mtg_utils/_regex_guard.py) — stdlib `re` has no timeout.
regex>=2024.4.16

"""deck-forge backend internals (state, signals, ranking, images, routes).

Mirrors the per-feature package convention used by ``_custom_format`` and
``_stores``. CLI-facing entry lives in ``mtg_utils.deck_forge_server``.
"""
